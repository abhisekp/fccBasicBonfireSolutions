var abhisekpFCCbasicBonfireSolutions = require(
        "../lib/fcc-basic-bonfire-solutions"),
    apFCCbbfs = abhisekpFCCbasicBonfireSolutions;

var chai = require("chai"),
    should = chai.should,
    expect = chai.expect,
    assert = chai.assert;



describe("Test Runs Successfully", function() {
    it("should return true", function() {

    });
});
